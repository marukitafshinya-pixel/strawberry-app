{
  "name": "strawberry-app",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project demo-ichigo \"vitest run tests\"",
    "emulators": "firebase emulators:start --project demo-ichigo"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.19.0",
    "firebase-tools": "^15.31.0",
    "vitest": "^5.0.1"
  }
}
